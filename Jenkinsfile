# add the stages and steps required to build the pipeline
pipeline{
  agent any
  stages{
    stage('Lint Docker File') {
      steps{
            # TODO
      }
    }
    stage('Build Dockerfile'){
      steps{
            # TODO
        }
      }
    }
  }
}
